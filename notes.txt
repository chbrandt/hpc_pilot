To-Do list
##########

- Add '--config' for api app launch
- Add interlink version to app config
- Add TLS
- Move away from NGINX (ingress)
- Add admins whitelist
- Add VO whitelist
- Add HPC install functionality
- Support unix sockets in wstunnel client
- Add wstunnel protocol to config (wss:443,ws:80)
- VK-nodes are in the global space (cluster-wise), restrict users able to use them
  - use taints to restrict pods scheduling
  - create a vk-node per hpc, with restrictions related to VO
  - create a vk-node per user per hpc, with restriction related to user/sub
  
