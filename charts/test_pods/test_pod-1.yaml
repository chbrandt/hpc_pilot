apiVersion: v1
kind: Pod
metadata:
  name: test-sleep
spec:
  nodeSelector:
    kubernetes.io/hostname: vk-node
  tolerations:
    - key: virtual-node.interlink/no-schedule
      operator: Exists
  containers:
    - name: test-sleep
      image: busybox
      command: ["sleep", "3600"]
