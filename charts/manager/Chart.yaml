apiVersion: v2
name: egi-hpc-pilot
version: 0.1.0
appVersion: "0.1.0"
type: application
description: >
  Helm chart for the HPC Pilot Manager — a Flask web application that manages
  InterLink pod deployments and HPC job submissions inside a Kubernetes cluster.
sources:
  - https://github.com/chbrandt/hpc_pilot
